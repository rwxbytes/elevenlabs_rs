[package]
name = "dialogue_karaoke"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
elevenlabs_rs = { path = "../../elevenlabs_rs", default-features = false, features = [
  "genai",
  "native-tls",
  "playback",
] }
tokio = { version = "1", features = ["full"] }
