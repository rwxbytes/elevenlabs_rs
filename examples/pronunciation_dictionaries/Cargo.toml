[package]
name = "pronunciation_dictionaries"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
elevenlabs_rs = { path = "../../elevenlabs_rs", default-features = false, features = [
  "admin",
  "genai",
  "native-tls",
  "playback",
] }
tokio = "1.42.0"

