#### Requirements

- SurrealDB,  [Check the installation guide](https://surrealdb.com/docs/surrealdb/installation)

#### Run
Before running cargo, in another terminal, run the SurrealDB server:
```
surreal start --user root --pass root --allow-all
```
